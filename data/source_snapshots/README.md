# Current source snapshots

This directory contains immutable source snapshots required by the active law-space manifests. They are provenance inputs to the current implementation and do not contain executable owner logic.
