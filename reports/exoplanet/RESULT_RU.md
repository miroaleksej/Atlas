# Проверка предоставленных наблюдений NASA

Локальный запуск использовал неизменённый `data/observations/nasa_exoplanet/PSCompPars_2026.09.20_07.33.39.csv`: 6366 строк, 84 столбца. SHA-256: `6129d26e657e1a26256b12fe151886fa28f5530e460578b5d68ab8bda939af9a`.

Это тот же снимок, который фигурирует в отчёте автора Deep Research patch. Повторное выполнение не является независимой репликацией или новой слепой проверкой. Данные не скачивались повторно: происхождение NASA установлено по заголовку предоставленного файла, а не по отдельной сетевой аттестации.

## Исправление отбора

При проверке обнаружено, что прежний `groupby.first()` мог заполнять пропущенные значения данными другой планеты той же звезды. Теперь после сортировки по хешу имени выбирается целая строка через `drop_duplicates`. Пропуски не заполняются. Регрессионный тест воспроизводит и предотвращает смешивание строк.

## Полученный результат

Поиск размерностной структуры сравнил 172 кандидата. На discovery-данных первым оказался вектор степеней `(3, -2, -1)`, то есть `a³/(P²M*)`. Он сохранил первое место на validation и sealed-разделах. Это контроль восстановления известной зависимости, а не новый закон.

| Раздел | Планет с положительными исходными величинами | Звёзд | Полных записей для анализа остатка |
|---|---:|---:|---:|
| Discovery | 3322 | 2447 | 1270 |
| Validation | 1154 | 844 | 425 |
| Sealed | 1117 | 836 | 447 |

Для остатка использованы 2142 целые строки после выбора одной планеты на звезду и исключения неконечных преобразованных координат. Это отличается от ранее опубликованного отчёта, в котором использовался прежний отбор.

Дополнительные оси не активированы. Итоговая NRMSE на validation — `1.0021215955`, на sealed — `1.0001851573`; обе совпадают с ошибкой исходной постоянной модели. Статус ядра: `REPRESENTATION_GAP_OPERATOR_PROBE_PROTOCOL_FROZEN_AWAITING_ATTESTED_RESPONSES`. Следующий эксперимент требует реально полученных ответов; они не выдуманы.

Полный машинный результат: `NASA_EXOPLANET_2026_LOCAL_RUN.json`. В унаследованном формате receipt значение `Infinity` у `expanded_holdout_nrmse` означает отсутствие принятого расширения, а не измеренную ошибку итоговой модели; фактические конечные оценки приведены выше. Чтение этого формата требует совместимого Python JSON reader; строгий JSON-reader может отклонить `Infinity`.

Новый закон, причинная переменная и научная новизна не установлены. Дальнейшие языковые оболочки в этот локальный запуск не включены. Исходный отчёт автора патча не перезаписан.
