# ACCEPTANCE_REPORT — ATLAS AI LAB EXTENSION 0.6.0

## Status

`PASS_STANDALONE_ATLAS_EXTENSION`

## Verified

- nested/standalone import: PASS;
- frozen Frontier 003 manifest: 168 model configurations;
- frozen results template: 672 measurement slots;
- required frontier capability contracts: 10;
- no-backend behavior: `MEASUREMENT_BACKEND_GAP`;
- targeted unit tests: 3/3 PASS;
- `compileall`: PASS;
- prebuilt wheel created and installed in a clean virtual environment: PASS;
- installed CLI `scienceatlas-ai-lab --help`: PASS;
- installed runtime status: `MEASUREMENT_BACKEND_GAP` as expected.

## Scientific boundary

No real model measurements are embedded in this extension. The extension provides the execution owner and frozen experiment interface. A real external backend is still required before Frontier 003 can produce scientific measurement receipts.

## v0.7.0 acceptance extension

Additional acceptance requirements:

- the original closed prospective loop completes on the declared toy fixture;
- Operator Genesis recovers the fixture family without generated Python;
- the entire genesis selection is calibrated by its permutation null;
- promoted synthesized owners enter quarantine and cannot self-credit;
- composite persistence preserves the runtime + extension root and rehydrates synthesized owners;
- three consecutive prospective failures revoke a synthesized owner without deleting its historical bus contract;
- mounting ScienceAtlas 15.17 is read-only;
- exactly the Atlas-qualified owner↔axis binding overlay is visible to the AI (three bindings in this release);
- those bindings do not become cross-domain routes;
- fixture observations are never treated as world evidence.


## 15.19.0 host integration acceptance

The extension remains version 0.7.0 at the runtime/state-schema layer, but the 15.19 host release adds the additive `epoch_genesis.py` orchestration owner and exact sequential permutation methodology. The sealed 15.18 `research_loop.py` remains authoritative for acquisition and evidence fingerprints. Under the qualified compute policy, the blind fixture yields `DEFERRED_RESOURCE` rather than an unverified promotion when the next exact sequential attempt requires 319 permutations.

## 15.19.1 package/evidence identity repair

The extension component identity is `0.9.0` consistently across directory, `pyproject.toml`, wheel METADATA, runtime package version and integration manifest. `AI_ACCEPTANCE_VERSION` remains `15.10.4`: this patch changes identity/evidence surfaces, not the accepted AI behaviour. Exact autonomous recovery of `2 - 7/x + 5xz` under the current valid sequential controller remains **UNVERIFIED** because the qualified path yields `DEFERRED_RESOURCE` before the required deeper search.


## v0.10.0 permutation e-process acceptance

The component adds persistent per-target e-process state and exact finite-group normalization. Qualified orbit calls read one frozen Genesis journal snapshot with writes disabled. The current empirical C6 Type-I sanity calibration is 15/1000 crossings over 1000 independent three-epoch noise paths. This is not a proof and not a system-wide guarantee.
